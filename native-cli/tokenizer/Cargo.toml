[package]
name = "ai_hider_tokenizer"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib", "rlib"]

[dependencies]
tokenizers = { version = "=0.22.2", default-features = false, features = ["onig"] }

[profile.release]
panic = "unwind"
